plugins {
    id 'org.springframework.boot' version '2.7.17'
    id 'java'
    id 'io.spring.dependency-management' version '1.1.3'
    // Recommended for dependency management
}

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'com.google.guava:guava:31.1-jre'
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}

java {
<<<<<<< HEAD
    sourceCompatibility = JavaVersion.VERSION_17
    targetCompatibility = JavaVersion.VERSION_17
=======
    sourceCompatibility = JavaVersion.VERSION_11
    targetCompatibility = JavaVersion.VERSION_11
>>>>>>> ce87f5a828c01db5347530080d18dcbc2cb81212
}

bootJar {
    mainClass = 'application.SpringApplication' // Update with your actual main class
}

jar {
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE

    manifest {
        attributes 'Main-Class': 'application.SpringApplication' // Update with your actual main class
    }

    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
}

tasks.withType(Test).configureEach {
    useJUnitPlatform() // Enables modern JUnit 5 testing platform if needed
    enabled = false    // Disable tests for faster builds (optional)
}
